/**
 * Canvas Layout Padding Configuration
 *
 * Controls the padding/margin around the product layout area.
 * This creates breathing room between the canvas edges and the products.
 */

export const CANVAS_PADDING_CONFIG = {
  /**
   * Top padding of the canvas layout area (in pixels)
   * Default: 20
   */
  paddingTop: 20,

  /**
   * Right padding of the canvas layout area (in pixels)
   * Default: 20
   */
  paddingRight: 30,

  /**
   * Bottom padding of the canvas layout area (in pixels)
   * Default: 20
   */
  paddingBottom: 0,

  /**
   * Left padding of the canvas layout area (in pixels)
   * Default: 20
   */
  paddingLeft: 30,

  /**
   * Gap between pivot groups/frames (in pixels)
   * Default: 40
   */
  frameGap: 20,

  /**
   * Gap between individual items/products (in pixels)
   * Default: 12
   */
  itemGap: 12,
};
